<div class="card shadow m-3 p-0" style="width: 21rem;">

    <img src={{dishImg}} class="card-img-top text-center dishImage" alt="..." />
    <div class="card-body">
        <h2 class="card-title text-center dishName">{{dishName}}</h2>
        <p class="card-text text-center">
          {{dishDesc}}
        </p>
    </div>

    <div class="form text-center">
          <label for="floatingEmptyPlaintextInput">
            <h3 class="dishPrice">${{dishPrice}}</h3>
          </label>
    </div>

    {{!-- <div class="container text-center">
        <button type="button" class="btn btn-light plus" id="plus-{{id}}">
      <i class="fa-solid fa-plus"></i>
      </button>

      <div class="form">
            <span class="quantity">0</span>
      </div>

      <button type="button" class="btn btn-light minus" id="minus-{{id}}">
      <i class="fa-solid fa-minus"></i>
      </button>
    </div> --}}

    <div class="card-body container text-center">
      <button type="button" class="btn btn-success shop-item-button" "{{id}}">Add to cart</button>
    </div>
</div>